<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="person.user.mapper.UserMapper">
    <select id="findAll" resultType="person.user.entity.User">
        SELECT * FROM tb_user
    </select>

    <update id="updateUser" resultType="person.user.entity.User">
        update
    </update>
</mapper>